<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="carlos.gofinance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CriarReceitaDespesaActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".activities.ConfirmacaoActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".activities.ListaActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".activities.CriarCartaoContaActivity"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>